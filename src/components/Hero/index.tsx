import { Container } from '../../global'
import { FormContainer, Form, HeroTitle } from './style'

const Hero = () => (
  <Form>
    <FormContainer >
      <HeroTitle >
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormContainer>
  </Form>
)

export default Hero
